// AI Modules Seeder - DISABLED (OpenAI not configured)
const seedAIModules = async () => {
  console.log('  → AI Modules seeding skipped (OpenAI not configured)');
  return;
};

module.exports = seedAIModules;